# Phase 1 (FP32 baseline training) — TensorFlow / Keras
tensorflow>=2.12
scikit-learn>=1.2

# Phase 2-4 (QAT, pruning, CoreML export) — PyTorch + Apple CoreML toolchain
torch>=2.0
coremltools>=7.0

# Common
numpy>=1.23
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
